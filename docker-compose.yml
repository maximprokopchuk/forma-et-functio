services:
  postgres:
    image: postgres:16
    container_name: forma-postgres
    environment:
      POSTGRES_USER: forma
      POSTGRES_PASSWORD: forma
      POSTGRES_DB: forma
    ports:
      - "5434:5432"
    volumes:
      - forma-data:/var/lib/postgresql/data
volumes:
  forma-data:
